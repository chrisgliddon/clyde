Run Mesen2 E2E tests for a project.

$ARGUMENTS (format: "<project> [test_name|all]")

Delegate to the **test-runner** agent to build and run the test(s). Review its summary. If failures are found, briefly state what failed and suggest next steps.
